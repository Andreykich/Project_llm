from .base_agent import BaseAgent
from config import config
import json
import logging

logger = logging.getLogger(__name__)

class DataAnalyzerAgent(BaseAgent):
    """
    Улучшенный агент анализа с поддержкой рассуждений (Reasoning)
    
    Философия работы:
    - Chain-of-Thought: делим сложные задачи на шаги
    - Context Engineering: используем максимум исторического контекста
    - Structured Output: всегда JSON с валидацией
    """
    
    def __init__(self, db=None):
        super().__init__(
            name="DataAnalyzer",
            system_prompt=config.PROMPTS["data_analyzer"]
        )
        self.db = db
        self.analysis_steps = []
    
    def generate_advice(self, user_data: dict) -> str:
        """
        Генерирует подробные рекомендации для бизнес-идеи.
        
        Строит Chain-of-Thought кцъп мышления для глубокого анализа.
        
        Args:
            user_data: Данные о бизнес-идее (industry, idea, city, budget, ит.d.)
            
        Returns:
            Полноценные рекомендации для пользователя
        """
        
        # Отслеживаем процесс мышления
        self.add_reasoning_step("Поначало: ранализ исходных данных")
        logger.info(f"[DataAnalyzer] Начинаю анализ данных: industry={user_data.get('industry')}, idea={user_data.get('idea')}")
        
        # Строим груструктурированный промпт с контекстом
        analysis_prompt = self._build_analysis_prompt(user_data)
        
        # Отправляем в LLM с Function Calling
        logger.info("[DataAnalyzer] Наступает анализ в LLM...")
        result = self.call_llm_with_tools(analysis_prompt, temperature=0.5)
        
        advice = result.get("response", "")
        reasoning = result.get("reasoning", "")
        
        if reasoning:
            self.add_reasoning_step(f"Объяснение LLM: {reasoning}")
        
        # Красивый вывод
        logger.info("[DataAnalyzer] Генерирование финального ответа")
        return self._format_advice(advice, user_data)
    
    def _build_analysis_prompt(self, user_data: dict) -> str:
        """
        Строит груструктурированный промпт с всем контекстом.
        
        На основе Лекции 7 - Context Engineering:
        - Главные данные использователя
        - Логика АНАЛИЗ -> РЕКОМЕНДАЦИИ
        - Жестко определенные разделы
        """
        
        template = f"""
## КОНКРЕТНЫЕ ДАННЫЕ ДЛЯ АНАЛИЗА

**Индустрия**: {user_data.get('industry', 'N/A')}
**Бизнес-идея**: {user_data.get('idea', 'N/A')}
**География**: {user_data.get('city', 'N/A')}
**Начальный бюджет**: {user_data.get('budget', 'N/A')}
**Опыт автора**: {user_data.get('experience', 'N/A')}
**Целевая аудитория**: {user_data.get('target_audience', 'N/A')}
**Особые пожелания**: {user_data.get('special_requirements', 'N/A')}

## ПОИНУК ДЛЯ АНАЛИЗА (строго ПО НАПОЛНЕНИЮ!):

### НАПОЛННИ КАК МИНИМУМ ТРИ ПУНКТА НЕ МЕНЕЕ

1. **ТРЕНД И РЫНОК** (МИН 3 пункта):
   - Конкретные тренды в {user_data.get('industry', 'этой сфере')} сейчас
   - Являются горячие ниши в этом сегменте?
   - Планы рынка на 2025 год

2. **ВОЗМОЖНОСТИ** (МИН 3 пункта):
   - УНИКАЛЬНОЕ ПО СРАВНЕНИЮ с конкурентами
   - МАСШТАбИРОВАННОсть идеи
   - ПОТЕНЦИАЛ ПАРТНЕРСТВ

3. **РИСКИ** (МИН 3 пункта):
   - КОНКУРЕНЦИИ и их силъ
   - БАРЬЕРЫ для входа
   - НОРМАтивНЫЕ РИСКИ

4. **ПЖАН ОТКРЫТИУ** (Специфика для {user_data.get('city', 'chosen_region')}):
   - МЕСЯЦы 1-3: [конкретные кновы]
   - МЕСЯЦы 4-6: [экспансия]
   - ГОД 2: [оптимизация]

5. **ФИНАНСОВАЯ СТРАТЕГИА**:
   - Аллокация бюджета (зная что {user_data.get('budget', 'budget_unknown')})
   - МАКСИМУМ ОМРОІ ОВ НА ROI
   - Ожидаемые метрики дохода

6. **МАРКЕТИНГ И ПОРОВОЖДОНИЕ**:
   - Лучшие каналы для {user_data.get('target_audience', 'target_audience')}
   - Бюджет на маркетинг
   - Тактики выхода

7. **КОНКУРЕНЦЫ И ПАРТНЕРЫ**:
   - 2-3 найвинованных конкурента: [назва + сильные стороны]
   - 2-3 потенциальных партнера: [как могут помогть]

8. **ИТОГОВАЯ ОЦЕНКА**:
   - ОЦЕНи ПЕРСПЕКТИВНОсть ИДЕИ: Очень горяче / Перспективно / Ок и т.d.
   - ОЦЕНи ВЕРОЯТНОсть МУГ УСПЕХА (год 1): 10-90%
   - НАВТОра - ОНВ НАЧНАФГ? [да/нет] + ГрАНдиозное незндка
"""
        return template
    
    def _format_advice(self, advice: str, user_data: dict) -> str:
        """
        Оформляет анализ в красивый формат для вывода пользователю.
        """
        formatted_output = f"""
🎯 **АНАЛИЗ: {user_data.get('idea', 'Your Business Idea')}**

{advice}

---
**Процесс анализа (для трассировки)**
{self.get_reasoning_trace()}
        """
        return formatted_output
